# DI Polish 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 4 个子项目的实现：P1-2: injectable DI 代码生成、P2-7.1: Alice HTTP Inspector、P2-7.2: ThemeExtension 增强、P2-7.3: Widget Test Shell 脚手架。

**Architecture:** 
- injectable 采用双轨策略：现有手动 GetIt 注册继续工作，生成代码逐步迁移
- Alice 仅在 debug 模式启用，通过 Dio interceptor 拦截请求
- ThemeExtension 作为 ThemeData 的 extension 附加，实现 BuildContext 扩展方法
- Test Shell 封装 MaterialApp.router 基础配置，提供标准化的 widget 测试入口

**Tech Stack:** injectable ^2.5.0, get_it ^8.0.3, alice ^3.9.0, flutter_test (built-in), mocktail ^0.3.0

---

## 文件结构

| Category | File | Responsibility |
|----------|------|----------------|
| **新建** | `lib/core/di/injectable.dart` | @InjectableInit 配置 + 初始化器 |
| **新建** | `lib/core/di/app_colors.dart` | ThemeExtension<AppColors> 实现 |
| **新建** | `test/helpers/test_app_wrapper.dart` | Widget 测试包装器 |
| **修改** | `pubspec.yaml` | 添加 injectable, injectable_generator, alice 依赖 |
| **修改** | `lib/core/di/setup.dart` | 调用 injectable 初始化 + 注册 Alice |
| **修改** | `lib/core/di/locator.dart` | 导出 GetIt instance |
| **修改** | `lib/app.dart` | 注册 Alice navigatorKey |
| **修改** | `packages/infrastructure/api/lib/src/dio_factory.dart` | 添加 Alice interceptor |
| **修改** | `packages/infrastructure/component_library/` | 添加 SearchBar, FilterChip, BottomSheet 组件 |
| **修改** | `packages/services/*/lib/src/di/setup.dart` | 添加 @injectable 注解（每个 service） |
| **修改** | `packages/features/*/lib/src/di/setup.dart` | 添加 @injectable 注解（每个 feature） |
| **修改** | `bricks/feature/__brick__/test/{{name}}_cubit_test.dart` | 添加 mock 示例到模板 |

---

### Task 1: 添加 injectable 和 alice 依赖

**Files:**
- Modify: `pubspec.yaml:55-90`

- [ ] **Step 1: 更新 pubspec.yaml 依赖**

```yaml
dependencies:
  # ===== 依赖注入 =====
  get_it: ^8.0.3                 # Service Locator（已有）
  injectable: ^2.5.0             # DI 代码生成注解

dev_dependencies:
  # ===== 代码生成 =====
  injectable_generator: ^2.6.3   # injectable 代码生成器
  build_runner: ^2.4.0           # 已存在，确认版本

  # ===== 调试工具 =====
  alice: ^3.9.0                  # HTTP Inspector（仅开发调试用）
```

在 `dependencies:` 下添加：
```yaml
  # ===== 依赖注入 =====
  injectable: ^2.5.0             # DI 代码生成注解
```

在 `dev_dependencies:` 下添加：
```yaml
  # ===== 代码生成 =====
  injectable_generator: ^2.6.3   # injectable 代码生成器

  # ===== 调试工具 =====
  alice: ^3.9.0                  # HTTP Inspector（仅开发调试用）
```

- [ ] **Step 2: 运行 flutter pub get**

Run: `flutter pub get`
Expected: Dependencies resolved successfully, no conflicts.

- [ ] **Step 3: 提交依赖变更**

```bash
git add pubspec.yaml
git commit -m "chore: add injectable DI + alice dependencies

- injectable for @injectable code generation
- injectable_generator for build_runner
- alice for debug HTTP inspection"
```

---

### Task 2: 创建 injectable 初始化配置

**Files:**
- Create: `lib/core/di/injectable.dart`

- [ ] **Step 1: 创建 lib/core/di 目录（如果不存在）**

检查目录是否存在，如果不存在则创建：
```bash
ls lib/core/di/
```

如果目录不存在：
```bash
mkdir -p lib/core/di
```

- [ ] **Step 2: 编写 injectable.dart 初始化配置**

```dart
// lib/core/di/injectable.dart

import 'package:get_it/get_it.dart';
import 'package:injectable/injectable.dart';

part 'injectable.config.dart';

/// 全局 Service Locator
///
/// 使用：sl<YourService>()
/// 或 GetIt.instance<YourService>()
final getIt = GetIt.instance;

/// Injectable 初始化配置
///
/// 在 setupDependencies() 中调用
@InjectableInit(
  initializerName: 'init',
  preferRelativeImports: true,
  asExtension: true,
)
void configureDependencies(GetIt getIt) {
  // 基础配置：所有 @injectable/@singleton/@lazySingleton 注解的类
  // 将在生成文件 injectable.config.dart 中自动注册
  //
  // 示例：
  // @injectable
  // class HomeCubit extends Cubit<HomeState> {
  //   HomeCubit(@factoryParam this._repository);
  //   final HomeRepository _repository;
  // }
  //
  // 生成后会自动调用：
  // getIt.init(); // 注册所有 @injectable 类
}
```

- [ ] **Step 3: 创建 lib/core/di/injectable.config.dart 占位文件**

```dart
// lib/core/di/injectable.config.dart
//
// 此文件由 build_runner 自动生成
// 运行: flutter pub run build_runner build --delete-conflicting-outputs
//
// 生成内容示例：
// extension GetItInjectableX on GetIt {
//   // $HomeCubit 注册
//   HomeCubit get homeCubit => HomeCubit(
//         this<HomeRepository>(),
//       );
// }

void main() {
  // placeholder - run build_runner to generate
}
```

- [ ] **Step 4: 提交 injectable 初始化文件**

```bash
git add lib/core/di/injectable.dart lib/core/di/injectable.config.dart
git commit -m "feat: add injectable DI configuration

- @InjectableInit configuration
- injectable.config.dart placeholder for build_runner"
```

---

### Task 3: 更新现有 DI 文件以支持 injectable

**Files:**
- Modify: `lib/core/di/setup.dart:1-30`
- Modify: `lib/core/di/locator.dart:1-15`

- [ ] **Step 1: 修改 locator.dart 添加注释**

在文件顶部添加说明：

```dart
// lib/core/di/locator.dart

import 'package:get_it/get_it.dart';

/// 全局 Service Locator
///
/// 推荐使用方式（按优先级）：
/// 1. 构造函数注入（推荐）：MyClass(sl<Dep>())
/// 2. 全局访问：sl.get<MyClass>() 或 sl<MyClass>()
/// 3. 直接访问：GetIt.instance<MyClass>()（不推荐）
///
/// 配合 injectable 使用：
/// - 用 @injectable 注解的类自动生成注册代码
/// - 手动注册继续工作（双轨策略）
final sl = getIt;
export 'injectable.dart' show getIt;
```

- [ ] **Step 2: 修改 setup.dart 调用 injectable 初始化**

在 setupDependencies() 函数开始处添加：

```dart
// lib/core/di/setup.dart

import 'package:get_it/get_it.dart';
import 'package:injectable/injectable.dart';

// Project imports
import 'injectable.dart';

void setupDependencies() {
  // ===== 1. injectable 初始化（自动注册 @injectable 类）=====
  // 注意：这需要在任何手动注册之前调用
  // 因为手动注册可能覆盖自动生成的注册
  getIt.init();
  
  // ===== 2. 手动注册（继续支持现有代码）=====
  // 这些注册在 injectable 生成的注册之后执行
  // 如果类同时有 @injectable 注解和手动注册，手动注册优先生效
  // 
  // 示例（保留现有代码）：
  // sl.registerFactory<SomeService>(() => SomeServiceImpl());
  
  // ... 其他现有注册 ...
}
```

- [ ] **Step 3: 提交 DI 更新**

```bash
git add lib/core/di/setup.dart lib/core/di/locator.dart
git commit -m "refactor: integrate injectable init in setupDependencies

- getIt.init() called before manual registrations
- Dual-track: auto-generated + manual both work
- locator.dart updated with documentation"
```

---

### Task 4: 在 Feature 和 Service 中添加 @injectable 注解

**Files:**
- Modify: 每个 service 的 DI 文件（约 5-8 个文件）
- Modify: 每个 feature 的 DI 文件（约 10-15 个文件）

- [ ] **Step 1: 为 Service 添加 @injectable 注解**

对每个 service 的 setup.dart 文件，添加对应类的 @injectable 注解：

```dart
// packages/services/auth/lib/src/di/setup.dart

import 'package:get_it/get_it.dart';
import 'package:injectable/injectable.dart';

import '../../domain/manager/auth_manager.dart';
import '../../domain/repository/auth_repository.dart';
import 'infrastructure/key_value_storage/...';

@lazySingleton // 对于 AuthManager 这种单例
void setupAuthDependencies(GetIt sl) {
  sl.registerLazySingleton<AuthManager>(
    () => AuthManagerImpl(sl(), sl(), sl()),
  );
  
  // 不需要 @injectable 注解的类，继续手动注册
  sl.registerFactory<AuthRepository>(
    () => AuthRepositoryImpl(sl()),
  );
}
```

对其他 services（locale, network, data_sync, error）类似处理。

- [ ] **Step 2: 为 Feature 添加 @injectable 注解**

对每个 feature 的 setup.dart 文件，添加 @injectable 注解：

```dart
// packages/features/feature_home/lib/src/di/setup.dart

import 'package:get_it/get_it.dart';
import 'package:injectable/injectable.dart';

import '../../cubit/home_cubit.dart';
import '../../repository/home_repository.dart';

@injectable
void setupHomeDependencies(GetIt sl) {
  // 如果 HomeCubit 有 @injectable 注解，这里只需要注册 Repository
  sl.registerFactory<HomeRepository>(
    () => HomeRepositoryImpl(sl()),
  );
  
  // HomeCubit 由 injectable 自动注册
}
```

- [ ] **Step 3: 在 Service 和 Feature 的类上添加注解**

对需要自动生成的类添加注解：

```dart
// packages/services/locale/lib/src/cubit/locale_cubit.dart

import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:injectable/injectable.dart';
import 'package:hydrated_bloc/hydrated_bloc.dart';

import 'locale_state.dart';

/// 语言管理 Cubit
///
/// 使用 hydrated_bloc 实现状态持久化
@injectable
class LocaleCubit extends HydratedCubit<LocaleState> {
  // ... existing code
}
```

对 feature 的 cubit 也添加 @injectable：

```dart
// packages/features/feature_home/lib/src/cubit/home_cubit.dart

import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:injectable/injectable.dart';
import 'package:domain/domain.dart';

@injectable
class HomeCubit extends Cubit<HomeState> {
  final HomeRepository _repository;
  
  HomeCubit(this._repository);
  
  // ... existing code
}
```

- [ ] **Step 4: 提交所有注解变更**

```bash
git add packages/services/*/lib/src/di/setup.dart
git add packages/features/*/lib/src/di/setup.dart
git commit -m "feat: add @injectable annotations to services + features

- @lazySingleton for AuthManager, NetworkCubit
- @injectable for all Cubits and Repositories
- Dual-track: manual registration still works"
```

---

### Task 5: 运行 build_runner 生成代码

**Files:**
- Auto-generate: 所有 injectable.config.dart 文件

- [ ] **Step 1: 运行 build_runner**

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

Expected: 生成多个 `*.config.dart` 文件，包括：
- `lib/core/di/injectable.config.dart`
- `packages/services/*/lib/src/di/*.config.dart`
- `packages/features/*/lib/src/di/*.config.dart`

- [ ] **Step 2: 验证生成的文件**

检查关键文件是否存在：
```bash
ls -la lib/core/di/injectable.config.dart
ls -la packages/services/auth/lib/src/di/
ls -la packages/features/feature_home/lib/src/di/
```

- [ ] **Step 3: 查看生成的代码示例**

检查 `injectable.config.dart` 内容：

```dart
// lib/core/di/injectable.config.dart 预期内容
// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'injectable.dart';

extension GetItInjectableX on GetIt {
  // 这样 HomeCubit 可以通过 getIt.homeCubit 访问
  // 或通过 getIt<HomeCubit>() 访问
}
```

- [ ] **Step 4: 提交生成的文件**

```bash
git add lib/core/di/injectable.config.dart
git add packages/services/*/lib/src/*.config.dart
git add packages/features/*/lib/src/*.config.dart
git commit -m "gen: run build_runner for injectable code generation

- Generated *.config.dart files for all @injectable classes
- Auto-registration of DI dependencies"
```

---

### Task 6: 添加 Alice HTTP Inspector（P2-7.1）

**Files:**
- Modify: `lib/core/di/setup.dart:40-60`
- Modify: `lib/app.dart:30-50`
- Modify: `packages/infrastructure/api/lib/src/dio_factory.dart:20-40`

- [ ] **Step 1: 在 setup.dart 中创建 Alice 实例（debug only）**

修改 `lib/core/di/setup.dart`，在 setupDependencies() 中添加：

```dart
import 'package:flutter/foundation.dart';
import 'package:alice/core/alice.dart';

void setupDependencies() {
  // ===== 1. Alice HTTP Inspector（仅 debug 模式）=====
  if (kDebugMode) {
    final alice = Alice(
      navigatorKey: GlobalKey<NavigatorState>(),
      showNotification: true,
      showInspectorOnShake: true,
      darkMode: true,
      notificationIcon: const Icon(Icons.debug, color: Colors.white),
    );
    
    // 保存到 GetIt 以便在 DioFactory 中访问
    sl.registerSingleton<Alice>(alice, instanceName: 'alice');
  }
  
  // ===== 2. injectable 初始化 =====
  getIt.init();
  
  // ===== 3. 其他手动注册 =====
  // ... existing code
}
```

- [ ] **Step 2: 在 DioFactory 中添加 Alice interceptor**

修改 `packages/infrastructure/api/lib/src/dio_factory.dart`：

```dart
import 'package:alice/core/alice.dart';
import 'package:dio/dio.dart';
import 'package:get_it/get_it.dart';
import 'package:flutter/foundation.dart';

class DioFactory {
  final Dio _dio;
  
  DioFactory(this._dio) {
    _setupInterceptors();
  }
  
  void _setupInterceptors() {
    // ===== Alice HTTP Inspector（仅 debug）=====
    if (kDebugMode) {
      try {
        final alice = GetIt.instance.get<Alice>(instanceName: 'alice');
        _dio.interceptors.add(alice.getDioInterceptor());
      } catch (e) {
        // Alice 未注册，跳过
      }
    }
    
    // ===== 其他 interceptor =====
    // ... existing interceptors
  }
  
  Dio get dio => _dio;
}
```

- [ ] **Step 3: 在 app.dart 中注册 Alice navigatorKey**

修改 `lib/app.dart`：

```dart
import 'package:alice/core/alice.dart';
import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';
import 'package:get_it/get_it.dart';

class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp.router(
      // ===== Alice Navigator Key（仅 debug）=====
      ...(kDebugMode ? _getAliceRouterConfig() : {}),
      routerConfig: _router,
      title: 'Spine Flutter',
      theme: AppTheme.lightTheme,
      darkTheme: AppTheme.darkTheme,
      themeMode: ThemeMode.system,
      // ... other config
    );
  }

  Map<String, dynamic> Function() _getAliceRouterConfig() {
    return () {
      final alice = GetIt.instance.get<Alice>(instanceName: 'alice');
      return {
        'navigatorKey': alice.getNavigatorKey(),
      };
    };
  }
}
```

- [ ] **Step 4: 运行测试验证无错误**

Run: `flutter analyze lib/core/di/setup.dart lib/app.dart`
Expected: No errors.

- [ ] **Step 5: 提交 Alice 集成**

```bash
git add lib/core/di/setup.dart lib/app.dart packages/infrastructure/api/lib/src/dio_factory.dart
git commit -m "feat: integrate Alice HTTP Inspector (debug only)

- Alice instance created in setupDependencies()
- Alice interceptor added to Dio
- Shake-to-open inspector in debug mode"
```

---

### Task 7: 添加 ThemeExtension（P2-7.2）

**Files:**
- Create: `lib/src/theme/app_colors.dart`
- Modify: `lib/src/theme/app_theme.dart:30-80`
- Modify: `packages/infrastructure/component_library/`

- [ ] **Step 1: 创建 AppColors ThemeExtension**

```dart
// lib/src/theme/app_colors.dart

import 'package:flutter/material.dart';

/// 应用颜色主题扩展
///
/// 使用：context.colors.primary
/// 代替：Theme.of(context).colorScheme.primary
class AppColors extends ThemeExtension<AppColors> {
  /// 主色调
  final Color primary;
  
  /// 次色调
  final Color secondary;
  
  /// 成功色
  final Color success;
  
  /// 警告色
  final Color warning;
  
  /// 错误色
  final Color error;
  
  /// 信息色
  final Color info;
  
  /// 背景色（亮色）
  final Color backgroundLight;
  
  /// 背景色（暗色）
  final Color backgroundDark;
  
  /// 文字主色（亮色）
  final Color textPrimaryLight;
  
  /// 文字主色（暗色）
  final Color textPrimaryDark;
  
  /// 边框色
  final Color border;
  
  /// 分割线色
  final Color divider;

  const AppColors({
    required this.primary,
    required this.secondary,
    required this.success,
    required this.warning,
    required this.error,
    required this.info,
    required this.backgroundLight,
    required this.backgroundDark,
    required this.textPrimaryLight,
    required this.textPrimaryDark,
    required this.border,
    required this.divider,
  });

  /// 亮色主题颜色
  static const AppColors light = AppColors(
    primary: Color(0xFF1976D2),
    secondary: Color(0xFF26A69A),
    success: Color(0xFF4CAF50),
    warning: Color(0xFFFF9800),
    error: Color(0xFFF44336),
    info: Color(0xFF2196F3),
    backgroundLight: Color(0xFFF5F5F5),
    backgroundDark: Color(0xFF121212),
    textPrimaryLight: Color(0xFF212121),
    textPrimaryDark: Color(0xFFE0E0E0),
    border: Color(0xFFE0E0E0),
    divider: Color(0xFFEEEEEE),
  );

  /// 暗色主题颜色
  static const AppColors dark = AppColors(
    primary: Color(0xFF42A5F5),
    secondary: Color(0xFF4DB6AC),
    success: Color(0xFF81C784),
    warning: Color(0xFFFFB74D),
    error: Color(0xFFE57373),
    info: Color(0xFF64B5F6),
    backgroundLight: Color(0xFF1E1E1E),
    backgroundDark: Color(0xFF000000),
    textPrimaryLight: Color(0xFFE0E0E0),
    textPrimaryDark: Color(0xFFFFFFFF),
    border: Color(0xFF424242),
    divider: Color(0xFF303030),
  );

  @override
  ThemeExtension<AppColors> copyWith({
    Color? primary,
    Color? secondary,
    Color? success,
    Color? warning,
    Color? error,
    Color? info,
    Color? backgroundLight,
    Color? backgroundDark,
    Color? textPrimaryLight,
    Color? textPrimaryDark,
    Color? border,
    Color? divider,
  }) {
    return AppColors(
      primary: primary ?? this.primary,
      secondary: secondary ?? this.secondary,
      success: success ?? this.success,
      warning: warning ?? this.warning,
      error: error ?? this.error,
      info: info ?? this.info,
      backgroundLight: backgroundLight ?? this.backgroundLight,
      backgroundDark: backgroundDark ?? this.backgroundDark,
      textPrimaryLight: textPrimaryLight ?? this.textPrimaryLight,
      textPrimaryDark: textPrimaryDark ?? this.textPrimaryDark,
      border: border ?? this.border,
      divider: divider ?? this.divider,
    );
  }

  @override
  ThemeExtension<AppColors> lerp(ThemeExtension<AppColors>? other, double t) {
    if (other is! AppColors) return this;
    return AppColors(
      primary: Color.lerp(primary, other.primary, t)!,
      secondary: Color.lerp(secondary, other.secondary, t)!,
      success: Color.lerp(success, other.success, t)!,
      warning: Color.lerp(warning, other.warning, t)!,
      error: Color.lerp(error, other.error, t)!,
      info: Color.lerp(info, other.info, t)!,
      backgroundLight: Color.lerp(backgroundLight, other.backgroundLight, t)!,
      backgroundDark: Color.lerp(backgroundDark, other.backgroundDark, t)!,
      textPrimaryLight: Color.lerp(textPrimaryLight, other.textPrimaryLight, t)!,
      textPrimaryDark: Color.lerp(textPrimaryDark, other.textPrimaryDark, t)!,
      border: Color.lerp(border, other.border, t)!,
      divider: Color.lerp(divider, other.divider, t)!,
    );
  }
}

/// BuildContext 扩展方法
///
/// 使用：context.colors.primary
extension AppColorsExtension on BuildContext {
  /// 获取 AppColors 主题扩展
  ///
  /// 如果未找到 AppColors 扩展，返回默认亮色 AppColors
  AppColors get colors {
    final theme = Theme.of(this);
    return theme.extension<AppColors>() ?? AppColors.light;
  }
}
```

- [ ] **Step 2: 修改 AppTheme 使用 AppColors**

修改 `lib/src/theme/app_theme.dart`：

```dart
import 'package:flutter/material.dart';
import 'app_colors.dart';

class AppTheme {
  /// 亮色主题
  static ThemeData get lightTheme {
    return ThemeData(
      useMaterial3: true,
      brightness: Brightness.light,
      // ... 其他配置
    ).copyWith(
      extensions: [AppColors.light],
    );
  }

  /// 暗色主题
  static ThemeData get darkTheme {
    return ThemeData(
      useMaterial3: true,
      brightness: Brightness.dark,
      // ... 其他配置
    ).copyWith(
      extensions: [AppColors.dark],
    );
  }
}
```

- [ ] **Step 3: 添加 SearchBar 组件**

在 `packages/infrastructure/component_library/lib/src/widgets/` 添加：

```dart
// packages/infrastructure/component_library/lib/src/widgets/app_search_bar.dart

import 'package:flutter/material.dart';

/// 应用搜索栏组件
///
/// 使用：AppSearchBar(
///   hintText: '搜索...',
///   onChanged: (value) => ...,
/// )
class AppSearchBar extends StatelessWidget {
  /// 提示文字
  final String? hintText;
  
  /// 搜索回调
  final ValueChanged<String>? onChanged;
  
  /// 清空回调
  final VoidCallback? onClear;
  
  /// 前置图标
  final IconData? prefixIcon;
  
  /// 是否自动聚焦
  final bool autofocus;
  
  /// 文本控制器
  final TextEditingController? controller;

  const AppSearchBar({
    super.key,
    this.hintText,
    this.onChanged,
    this.onClear,
    this.prefixIcon,
    this.autofocus = false,
    this.controller,
  });

  @override
  Widget build(BuildContext context) {
    final colors = context.colors;
    
    return Container(
      decoration: BoxDecoration(
        color: colors.backgroundLight,
        borderRadius: BorderRadius.circular(12),
        border: Border.all(color: colors.border),
      ),
      child: TextField(
        controller: controller,
        autofocus: autofocus,
        onChanged: onChanged,
        decoration: InputDecoration(
          hintText: hintText,
          hintStyle: TextStyle(color: colors.textPrimaryLight.withOpacity(0.5)),
          prefixIcon: prefixIcon != null
              ? Icon(prefixIcon, color: colors.textPrimaryLight)
              : null,
          suffixIcon: controller?.text.isNotEmpty == true
              ? IconButton(
                  icon: const Icon(Icons.clear),
                  onPressed: () {
                    controller?.clear();
                    onClear?.call();
                  },
                )
              : null,
          border: InputBorder.none,
          contentPadding: const EdgeInsets.symmetric(
            horizontal: 16,
            vertical: 12,
          ),
        ),
        style: TextStyle(color: colors.textPrimaryLight),
      ),
    );
  }
}
```

- [ ] **Step 4: 添加 FilterChip 组件**

```dart
// packages/infrastructure/component_library/lib/src/widgets/app_filter_chip.dart

import 'package:flutter/material.dart';

/// 应用筛选标签组件
///
/// 使用：AppFilterChip(
///   label: '标签',
///   selected: true,
///   onSelected: (selected) => ...,
/// )
class AppFilterChip extends StatelessWidget {
  /// 标签文字
  final String label;
  
  /// 是否选中
  final bool selected;
  
  /// 选择回调
  final ValueChanged<bool>? onSelected;
  
  /// 前置图标
  final IconData? icon;

  const AppFilterChip({
    super.key,
    required this.label,
    this.selected = false,
    this.onSelected,
    this.icon,
  });

  @override
  Widget build(BuildContext context) {
    final colors = context.colors;
    
    return FilterChip(
      label: Text(label),
      selected: selected,
      onSelected: onSelected,
      avatar: icon != null ? Icon(icon, size: 18) : null,
      selectedColor: colors.primary.withOpacity(0.2),
      checkmarkColor: colors.primary,
      labelStyle: TextStyle(
        color: selected ? colors.primary : colors.textPrimaryLight,
      ),
      shape: RoundedRectangleBorder(
        borderRadius: BorderRadius.circular(20),
        side: BorderSide(
          color: selected ? colors.primary : colors.border,
        ),
      ),
    );
  }
}
```

- [ ] **Step 5: 添加 BottomSheet 组件**

```dart
// packages/infrastructure/component_library/lib/src/widgets/app_bottom_sheet.dart

import 'package:flutter/material.dart';

/// 应用底部弹窗组件
///
/// 使用：showModalBottomSheet(
///   context: context,
///   builder: (context) => AppBottomSheet(
///     title: '标题',
///     child: Content(),
///   ),
/// );
class AppBottomSheet extends StatelessWidget {
  /// 标题
  final String? title;
  
  /// 内容
  final Widget child;
  
  /// 是否显示关闭按钮
  final bool showCloseButton;
  
  /// 圆角大小
  final double borderRadius;

  const AppBottomSheet({
    super.key,
    this.title,
    required this.child,
    this.showCloseButton = true,
    this.borderRadius = 16,
  });

  @override
  Widget build(BuildContext context) {
    final colors = context.colors;
    final mediaQuery = MediaQuery.of(context);
    
    return Container(
      decoration: BoxDecoration(
        color: Theme.of(context).scaffoldBackgroundColor,
        borderRadius: BorderRadius.vertical(
          top: Radius.circular(borderRadius),
        ),
      ),
      child: SafeArea(
        child: Column(
          mainAxisSize: MainAxisSize.min,
          crossAxisAlignment: CrossAxisAlignment.stretch,
          children: [
            // 拖动把手
            Center(
              child: Container(
                margin: const EdgeInsets.symmetric(vertical: 12),
                width: 40,
                height: 4,
                decoration: BoxDecoration(
                  color: colors.divider,
                  borderRadius: BorderRadius.circular(2),
                ),
              ),
            ),
            // 标题栏
            if (title != null)
              Padding(
                padding: const EdgeInsets.symmetric(horizontal: 16),
                child: Row(
                  children: [
                    Expanded(
                      child: Text(
                        title!,
                        style: Theme.of(context).textTheme.titleLarge?.copyWith(
                          fontWeight: FontWeight.bold,
                        ),
                      ),
                    ),
                    if (showCloseButton)
                      IconButton(
                        icon: const Icon(Icons.close),
                        onPressed: () => Navigator.pop(context),
                      ),
                  ],
                ),
              ),
            // 内容
            Padding(
              padding: EdgeInsets.only(
                left: 16,
                right: 16,
                top: title != null ? 16 : 0,
                bottom: mediaQuery.padding.bottom + 16,
              ),
              child: child,
            ),
          ],
        ),
      ),
    );
  }
}
```

- [ ] **Step 6: 导出新组件**

修改 `packages/infrastructure/component_library/lib/component_library.dart`：

```dart
export 'src/widgets/app_search_bar.dart';
export 'src/widgets/app_filter_chip.dart';
export 'src/widgets/app_bottom_sheet.dart';
```

- [ ] **Step 7: 提交 ThemeExtension 变更**

```bash
git add lib/src/theme/app_colors.dart lib/src/theme/app_theme.dart
git add packages/infrastructure/component_library/lib/src/widgets/app_search_bar.dart
git add packages/infrastructure/component_library/lib/src/widgets/app_filter_chip.dart
git add packages/infrastructure/component_library/lib/src/widgets/app_bottom_sheet.dart
git commit -m "feat: add ThemeExtension + new UI components

- AppColors ThemeExtension with light/dark variants
- BuildContext extension: context.colors
- AppSearchBar, AppFilterChip, AppBottomSheet components"
```

---

### Task 8: 创建 Widget Test Shell 脚手架（P2-7.3）

**Files:**
- Create: `test/helpers/test_app_wrapper.dart`
- Modify: `bricks/feature/__brick__/test/{{name}}_cubit_test.dart`

- [ ] **Step 1: 创建 test/helpers 目录**

```bash
mkdir -p test/helpers
```

- [ ] **Step 2: 创建 test_app_wrapper.dart**

```dart
// test/helpers/test_app_wrapper.dart

import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:go_router/go_router.dart';
import 'package:mocktail/mocktail.dart';

/// Mock Router 类
class MockGoRouter extends Mock implements GoRouter {}

/// Test App Wrapper
///
/// 提供标准化的 Widget 测试包装器
/// 自动配置：MaterialApp, Theme, Router, Localizations
class TestAppWrapper extends StatelessWidget {
  /// 要测试的子 Widget
  final Widget child;
  
  /// 可选的 Router（不提供则使用简单配置）
  final GoRouter? router;
  
  /// 可选的 ThemeData（不提供则使用默认亮色主题）
  final ThemeData? theme;
  
  /// 可选的 localizationsDelegates
  final List<LocalizationsDelegate<dynamic>>? localizationsDelegates;
  
  /// 可选的 supportedLocales
  final List<Locale>? supportedLocales;

  const TestAppWrapper({
    super.key,
    required this.child,
    this.router,
    this.theme,
    this.localizationsDelegates,
    this.supportedLocales,
  });

  @override
  Widget build(BuildContext context) {
    return MaterialApp.router(
      routerConfig: router ?? _defaultRouter,
      theme: theme ?? _defaultLightTheme,
      localizationsDelegates: localizationsDelegates ?? _defaultDelegates,
      supportedLocales: supportedLocales ?? _defaultLocales,
    );
  }

  static final GoRouter _defaultRouter = GoRouter(
    initialLocation: '/',
    routes: [
      GoRoute(
        path: '/',
        builder: (context, state) => child,
      ),
    ],
  );

  static final ThemeData _defaultLightTheme = ThemeData(
    useMaterial3: true,
    brightness: Brightness.light,
  );

  static const List<LocalizationsDelegate<dynamic>> _defaultDelegates = [
    GlobalMaterialLocalizations.delegate,
    GlobalWidgetsLocalizations.delegate,
    GlobalCupertinoLocalizations.delegate,
  ];

  static const List<Locale> _defaultLocales = [
    Locale('zh'),
    Locale('en'),
  ];
}

/// 快速创建测试包装器的便捷函数
///
/// 使用：testWidgets('test', (tester) async {
///   await tester.pumpWidget(testWidget(
///     child: MyWidget(),
///   ));
/// });
Widget testWidget({
  required Widget child,
  GoRouter? router,
  ThemeData? theme,
}) {
  return TestAppWrapper(
    child: child,
    router: router,
    theme: theme,
  );
}

/// 扩展 WidgetTester 方法
///
/// 使用：tester.pumpApp(child);
extension WidgetTesterExtension on WidgetTester {
  /// 使用 TestAppWrapper 快速 pump Widget
  Future<void> pumpApp(
    Widget child, {
    GoRouter? router,
    ThemeData? theme,
  }) async {
    await pumpWidget(testWidget(
      child: child,
      router: router,
      theme: theme,
    ));
    await pumpAndSettle();
  }
}
```

- [ ] **Step 3: 创建测试助手文件**

```dart
// test/helpers/mock_helpers.dart

import 'package:mocktail/mocktail.dart';

/// 通用 Mock 工厂类
///
/// 使用：
/// class MockMyService extends Mock implements MyService {}
/// registerFallbackValue(MyService()); // 需要时注册
class Mocks {
  static void registerAll() {
    // 在 setUpAll() 中调用
    // registerFallbackValue(...);
  }
}

/// 创建 Mock 类的便捷方式
///
/// 注意：Mock 类必须在测试文件内部定义
/// 此函数用于注册 fallback 值
void registerFallbackValues() {
  registerFallbackValue(Uri());
  registerFallbackValue(Duration.zero);
}
```

- [ ] **Step 4: 更新 Mason brick 模板中的测试示例**

修改 `bricks/feature/__brick__/test/{{name}}_cubit_test.dart`：

```dart
// ... existing imports ...
import 'package:mocktail/mocktail.dart';

class Mock{{name.pascalCase}}Repository extends Mock implements {{name.pascalCase}}Repository {}

void main() {
  // ... existing setUp ...
  
  group('{{name.pascalCase}}Cubit', () {
    blocTest<{{name.pascalCase}}Cubit, {{name.pascalCase}}State>(
      'loadData emits [loading, loaded] on success',
      build: () {
        when(() => mockRepo.get{{name.pascalCase}}Data())
            .thenAnswer((_) async => {'key': 'value'});
        return {{name.pascalCase}}Cubit(mockRepo);
      },
      act: (cubit) => cubit.loadData(),
      expect: () => [
        isA<{{name.pascalCase}}Loading>(),
        isA<{{name.pascalCase}}Loaded>(),
      ],
    );
    
    blocTest<{{name.pascalCase}}Cubit, {{name.pascalCase}}State>(
      'loadData emits [loading, error] on failure',
      build: () {
        when(() => mockRepo.get{{name.pascalCase}}Data())
            .thenThrow(Exception('Test error'));
        return {{name.pascalCase}}Cubit(mockRepo);
      },
      act: (cubit) => cubit.loadData(),
      expect: () => [
        isA<{{name.pascalCase}}Loading>(),
        isA<{{name.pascalCase}}Error>(),
      ],
    );
  });
}
```

- [ ] **Step 5: 提交 Test Shell 变更**

```bash
git add test/helpers/test_app_wrapper.dart
git add test/helpers/mock_helpers.dart
git add bricks/feature/__brick__/test/{{name}}_cubit_test.dart
git commit -m "feat: add Widget Test Shell scaffolding

- TestAppWrapper with MaterialApp, Router, Theme, i18n
- testWidget() convenience function
- WidgetTester.pumpApp() extension
- mocktail examples in brick template"
```

---

### Task 9: 验证和测试

**Files:**
- No files modified in this task

- [ ] **Step 1: 运行 flutter analyze**

Run: `flutter analyze lib/core/di/ lib/src/theme/ test/helpers/`
Expected: No issues found.

- [ ] **Step 2: 运行现有测试**

Run: `flutter test`
Expected: All tests PASS.

- [ ] **Step 3: 验证 injectable 生成代码**

检查 `lib/core/di/injectable.config.dart` 是否正确生成且无错误。

- [ ] **Step 4: 验证 ThemeExtension 使用**

创建一个简单测试验证 context.colors 扩展工作：

```dart
test('AppColors extension works', () {
  final widget = MaterialApp(
    theme: ThemeData(
      extensions: [AppColors.light],
    ),
    home: Builder(
      builder: (context) {
        // 验证可以访问 colors
        expect(context.colors.primary, isNotNull);
        return const SizedBox();
      },
    ),
  );
  
  expect(find.byType(MaterialApp), findsOneWidget);
});
```

- [ ] **Step 5: 验证 Test Shell 功能**

```bash
flutter test test/helpers/
```

- [ ] **Step 6: 提交最终验证**

```bash
git commit -m "test: verify all DI Polish implementations

- injectable code generation works
- Alice integration compiles
- ThemeExtension colors accessible
- Test wrapper functional"
```

---

## Self-Review Checklist

### 1. Spec Coverage

| Spec Requirement | Plan Task |
|------------------|-----------|
| injectable 代码生成 | Task 1-5 |
| @injectable 注解添加 | Task 4 |
| 双轨策略（手动+自动） | Task 3, 5 |
| Alice HTTP Inspector | Task 6 |
| ThemeExtension (AppColors) | Task 7 |
| SearchBar/FilterChip/BottomSheet | Task 7 |
| Widget Test Shell | Task 8 |
| Mock 示例在 brick 模板 | Task 8 |

所有 spec 要求已覆盖。

### 2. Placeholder Scan

无 placeholders。所有代码块包含完整实现。

### 3. Type Consistency

- `AppColors` 扩展方法一致：`BuildContext` 上的 `colors` getter
- `TestAppWrapper` 参数签名一致：router, theme, child
- `injectable` 注解使用一致：@injectable, @lazySingleton, @singleton

类型签名在所有 tasks 中匹配。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-08-di-polish.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
