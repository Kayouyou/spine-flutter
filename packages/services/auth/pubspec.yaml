name: auth
description: Authentication service
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.2.0+1
  domain:
    path: ../../domain
  key_value_storage:
    path: ../../infrastructure/key_value_storage
  get_it: ^7.6.0
  flutter_bloc: ^9.1.0
  freezed_annotation: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^10.0.0
  flutter_lints: ^4.0.0
  mocktail: ^1.0.0
  build_runner: ^2.4.0
  freezed: ^2.4.0