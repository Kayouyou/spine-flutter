name: auth
description: Authentication service
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.2.0+1
  domain:
    path: ../../domain
  key_value_storage:
    path: ../../infrastructure/key_value_storage
  get_it: ^7.6.0
  flutter_bloc: ^9.1.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^0.3.0