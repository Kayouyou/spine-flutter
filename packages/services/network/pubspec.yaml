name: network
description: 全局网络连接状态监听服务。
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.0
  connectivity_plus: ^6.0.0
  equatable: ^2.0.5
