name: feature_settings
description: Settings 功能模块
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  get_it: ^7.7.0
  go_router: ^14.2.7
  routing:
    path: ../../infrastructure/routing
  component_library:
    path: ../../infrastructure/component_library
  domain:
    path: ../../domain
  locale:
    path: ../../services/locale
  auth:
    path: ../../services/auth

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
