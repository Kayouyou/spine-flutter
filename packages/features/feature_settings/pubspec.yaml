name: feature_settings
description: Settings feature module (UI extraction from routing)
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  component_library:
    path: ../../infrastructure/component_library

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0