name: domain
description: 纯 Dart 业务领域层 — 数据模型、仓储接口、用例、异常定义。
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  equatable: ^2.0.5
  freezed_annotation: ^2.4.0
  json_annotation: ^4.9.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.24.0
  mocktail: ^1.0.5
  build_runner: ^2.4.9
  freezed: ^2.5.2
  json_serializable: ^6.7.1
